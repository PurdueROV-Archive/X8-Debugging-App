<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity"
    android:background="#000000">

    <!--Title, appears at the top-->
    <TextView android:id="@+id/title"
        android:text="ROV Debugger"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#FFFFFF"/>
    <!--instructions, appears right above the bluetooth connection dropdown menu-->
    <TextView android:id="@+id/instructions" 
        android:text="connect to ROV"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#FFFFFF"
        android:layout_below="@id/title"/>
    <!--bluetooth dropdown menu, appears in the middle-->
    <Spinner android:id="@+id/bluetoothConnectionsList"
        android:layout_width="fill_parent"
        android:layout_height="16dp">
    </Spinner>
    <!--version number, appears at bottom left corner-->
    <TextView android:id="@+id/versionNum"
        android:text="V1.0"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#FFFFFF"
        android:layout_alignParentLeft="true"
        android:layout_alignParentBottom="true"/>
    <!--last updated date, appears at bottom right corner-->
    <TextView android:id="@+id/upDate"
        android:text="Last updated: Oct, 2015"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#FFFFFF"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true"/>
</RelativeLayout>
